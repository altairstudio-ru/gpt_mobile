PR #263 review follow-up on branch fix/safer-image-upload-pipeline: validated and fixed these issues after first-pass implementation — non-image attachments are now rejected in ChatViewModel and skipped in ChatRepositoryImpl; queued send resumes correctly after rejection/failure and restores queued text; total draft attachment size is enforced at 50 MB across all selected attachments; oversize/unsupported draft removals clean up draft files; AttachmentPayloadCache is now bounded with LRU eviction (MAX_ENTRIES=32), has clear(), and ChatViewModel clears it in onCleared(); FileUtils now writes resized copies to context.cacheDir and resolves content-URI display names safely for temp filenames; docs plan headings changed from H3 to H2 for markdownlint; added tests for FileUtils total-limit helpers and AttachmentPayloadCache eviction.