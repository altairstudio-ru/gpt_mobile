On branch `fix/safer-image-upload-pipeline`, wrote spec+plan docs under docs/superpowers/{specs,plans}. Implemented first pass of safer attachment preprocessing: new `ChatAttachmentDraft` UI state, `AttachmentPayloadCache`, FileUtils global 50MB limit + conditional resize helpers, ViewModel async attachment preprocessing and queued-send behavior, repository attachment encoding via cache/background IO. Verification run: `./gradlew :app:testDebugUnitTest :app:compileDebugKotlin` passed. Remaining limitations: ChatScreen still copies selected files synchronously before ViewModel preprocessing, and queued send shows loading/disabled send while attachments prepare rather than inserting a provisional user message into chat history immediately.